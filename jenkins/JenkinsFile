pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Realiza o checkout do repositório contendo os scripts Python
                git 'https://github.com/rmohandasrodrigues/clientes.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                // Atualiza os pacotes e instala o Python3
                sh 'apt-get update && apt-get install -y python3'
            }
        }

        stage('Install Python Packages') {
            steps {
                // Comando de exemplo - mudando de diretório
                sh 'cd /'
            }
        }

        stage('Run Extraction Script') {
            steps {
                // Corrige erro de digitação em "mkdir" e o nome do diretório
                sh 'mkdir meu_pal'
            }
        }

        stage('Send to Kafka') {
            steps {
                // Corrige erro de digitação em "mkdir" e o nome do diretório
                sh 'mkdir tomar_no_cu'
            }
        }
    }

    post {
        success {
            echo 'Pipeline executado com sucesso!'
        }
        failure {
            echo 'Pipeline falhou!'
        }
    }
}
